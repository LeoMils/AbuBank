node_modules/
dist/
.env
.env.*
.env.local
.env.txt
*.local

.vercel

.ai-runs/

# Voice pipeline diagnostic harness output — regenerated on every test run.
# The harness code under src/screens/AbuCalendar/diagnostics/ IS tracked;
# only the generated artifact is ignored because it embeds wall-clock times.
docs/voice-pipeline/voice-pipeline-diagnostic.txt

# AbuWhatsApp local-only private contact data.
# Real phone numbers must never be committed; if an operator drops a JSON
# backup into the repo by accident, these patterns block it from staging.
private/
*.local.json
tmp/dev-cert/

# Capacitor
ios/App/App/public/
ios/App/Pods/
android/app/src/main/assets/public/
openai api.txt
tmp/
test-results/

# Eval engine regenerated state (reports are committed, baseline is local)
docs/eval/baseline.json

# AbuAI parity program single-writer execution lock (local lease, never committed)
.abuai/ACTIVE_EXECUTION_LOCK.json
.claude/settings.local.json
.abuai-live-parity-v4-backups/
evals/live-parity-lab/private-recordings/
evals/live-parity-lab/raw-user-audio/

# Abu production gate — generated cache + Stop-guard state (not source)
docs/engineering-os/qa/production-convergence/gate-status.json
.claude/.abu-goal-active
.claude/.abu-stop-block-count

docs/engineering-os/qa/production-convergence/verdict.json
