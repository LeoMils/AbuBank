# CLAUDE CODE — FINAL ABUAI CHATGPT LIVE PARITY LAUNCH

Read `ABUAI_CHATGPT_LIVE_PARITY_PROGRAM.md` completely and execute it as the controlling program for AbuAI.

This is the final pre-execution mandate. Do not rewrite it, replace it with another framework, or stop to design a new specification hierarchy. Preserve all existing verified work and use the program to produce one real evidence-backed product recovery cycle.

If any wording in this launch prompt differs from the controlling program, the program governs.

## 1. Single-writer safety first

Before any repository mutation:

1. locate and confirm the correct repository root;
2. inspect the current branch, commit, upstream, `git status`, uncommitted changes, recent commits, worktrees, merge/rebase state, and repository-visible execution markers;
3. inspect `.abuai/ACTIVE_EXECUTION_LOCK.json` if present;
4. determine whether another local, cloud, web, CI, or other-clone writer is active or plausibly active;
5. stop before mutation if safe single-writer ownership cannot be established;
6. otherwise acquire and verify the lock exactly as defined in the controlling program.

Do not use `/batch`, `/background`, `/fork`, worktrees, detached mutation, parallel writers, or uncontrolled loops. Note two current Claude Code realities: subagents run in the background by default, so any subagent dispatch counts as background execution unless verified otherwise in the installed version; and `/doctor` can apply fixes, so use it in report-only fashion and decline all offered fixes before lock acquisition. Bounded read-only specialist analysis is allowed only under the controlling program’s restrictions and only with verified foreground execution. The foreground session remains the sole writer and decision authority. Where the permission system supports it, add deny rules and a PreToolUse hook enforcing these prohibitions before the first mutation.

## 2. Reconcile before creating

After lock ownership is established:

1. read all applicable `CLAUDE.md` and project rules;
2. read the authoritative Acceptance Board, Engineering OS, recovery, replay, evaluation, voice, online, family, calendar, memory, evolution, deployment, security, diagnostics, Skills, Hooks, and prior investigations;
3. inspect the actual reachable code and runtime;
4. classify each material requirement as verified existing, existing but unverified, partial, equivalent or better, obsolete, missing, conflicting, or unknown;
5. reuse valid assets;
6. update one existing source of truth rather than creating renamed duplicates.

Do not restart solved work. Do not preserve stale conclusions. Do not create a second architecture, memory system, orchestration layer, evidence board, or documentation hierarchy.

## 3. Verify volatile capabilities

Use current primary official sources and actual environment detection to verify only facts that can change a material decision, including:

- current ChatGPT Live behavior and limitations;
- actual GPT-Live API entitlement, if any;
- accessible OpenAI Realtime models;
- transport and session constraints;
- installed Claude Code version and available capabilities;
- browser/mobile restrictions;
- provider credentials, quotas, and reachable test environments.

Never expose secret values. Do not select a model or architecture by name alone.

## 4. Measure the real product

Run the real user-facing microphone-to-speaker path with safe non-production data.

Establish the strongest reachable baseline for:

- session entry and readiness;
- listening and semantic turn completion;
- speaking while listening;
- interruption, backchannels, and overlap;
- first-audible-response and tail latency;
- voice, pacing, prosody, warmth, and intelligibility;
- Hebrew, Spanish, and code-switching;
- context and corrections;
- family and grammatical gender;
- persistent memory;
- calendar create/read/modify/delete and exactly-once behavior;
- current online information;
- emotional conversation;
- failure recovery;
- mobile and long-session lifecycle.

Do not treat configuration, transcript generation, provider response, unit tests, or desktop behavior as proof of audible target-device experience.

You have no microphone, speaker, physical device, or ears. Where the microphone-to-speaker path requires a human or a physical device, emit an Operator Protocol as defined by the controlling program (`diagnostics/operator-protocols/<id>.md`, numbered steps, structured response template) and continue with machine-provable work. Do not stall on unavailable device evidence and do not substitute fixtures, transcripts, or desktop runs as device proof.

## 5. Select exactly one recovery cycle

Choose the highest-severity real user-visible RED behavior. If the top-ranked RED behaviors require human or device evidence unavailable in this session, choose the highest-severity RED behavior provable to at least AUTOMATED_TEST or PREVIEW_VERIFIED without a human (for example calendar exactly-once under retry/reconnect, locale contamination, or a family/gender regression at the text layer), and emit Operator Protocols for the device-dependent items in parallel. This session must not end with zero completed recovery cycles.

For that behavior:

1. reproduce it;
2. define the expected observable behavior;
3. locate the first divergence;
4. identify the root-cause hypothesis;
5. define the acceptance oracle and strongest reachable evidence class;
6. add a regression, replay, acceptance test, or precise human/device protocol;
7. implement the smallest correct fix;
8. avoid broad refactors or migrations unless the current architecture is the proven first divergence and a smaller correction cannot succeed.

Do not switch to another issue until this one is GREEN at the reachable evidence level or concretely blocked by an external dependency.

## 6. Validate honestly

Run the relevant validation ladder defined by the controlling program:

- targeted regression;
- integration;
- replay;
- relevant full test suite;
- build;
- preview;
- real audio path;
- physical device where available;
- latency distribution;
- security/privacy review;
- duplicate-action and reconnect safety.

Use exact evidence classes. Never convert CODE, TEST, PREVIEW, generated transcript, or lack of observed failure into DEVICE or PRODUCTION proof.

Never claim “identical,” “100% parity,” “production-ready,” “fixed,” “verified,” or “green” beyond the evidence actually obtained.

## 7. Commit and continue

When one coherent recovery cycle is green:

1. update the authoritative Acceptance Board and evidence;
2. record baseline, first divergence, root cause, change, validation, residual gap, and rollback;
3. commit and push only to the authorized feature branch;
4. never merge to main;
5. never deploy production without explicit approval;
6. release or hand off the execution lock safely.

If the selected issue is externally blocked, complete all safe preparation, instrumentation, regression construction, and exact handoff instead of fabricating completion.

## 8. Final output

Return only the report required by `ABUAI_CHATGPT_LIVE_PARITY_PROGRAM.md`, including:

- actual improvement or blocker;
- branch and commit;
- lock disposition;
- Acceptance Board;
- exact evidence classes;
- regression, replay, tests, build, preview, device, voice, and latency results;
- unresolved RED items and real risks;
- exactly one continuation prompt.

Do not provide another preliminary architecture review.

Start now with the read-only controlling-program load and the single-writer safety phase, then continue through one complete evidence-producing recovery cycle.
