{
  "objective": "Wire + prove the first generalized Realtime vertical slice (ADR-0001 GRA-EDC): one continuous Hebrew Realtime conversation; one greeting/session; WhatsApp action via typed tool+kernel; in-session live card; 'לא, תתקשרי אליו' atomically replaces WhatsApp->Call; no stale speech/card; UI+speech one revision; no fabricated fact/completion; fallback preserves authorities.",
  "governingADR": "docs/engineering-os/adr/ADR-0001-abu-realtime-conversational-architecture.md (CERTIFIED)",
  "authorityModel": { "TALK": "gpt-realtime (model)", "STATE": "control plane (deterministic)", "TRUTH/ACTION": "kernel reduceGoal + tools", "UI": "projection of committed state" },
  "currentCommit": "pending-this-commit (builds on 683f3c1)",
  "completedStages": [
    "S1 control plane (STATE authority) — src/screens/AbuAI/realtime/controlPlane.ts — 16/16",
    "S4a tool dispatch (delegates to kernel; no completion; privacy; idempotency) — realtimeTools.ts — 9/9",
    "S7a streaming-truth monitor (completion + unsupported-denial) — truthMonitor.ts — 6/6",
    "S5a simulated-realtime seam: session orchestrator composing STATE+TRUTH+SPEECH-GUARD with injected events + canonical ActiveActionViewModel (§13) — sessionOrchestrator.ts — 9/9; production kernel adapter over buildCommunicationAction — kernelAdapter.ts. §18 journey provable WITHOUT a mic. 40 tests total; typecheck 0 errors.",
    "S5b canonical ActiveActionCard (§13 projection) + realtime2 slice flag (independent, OFF by default) + self-contained mic-free RealtimeSliceHarness mounted behind isRealtimeSliceEnabled() (two gated lines; certified path untouched). Version 0.169.0->0.170.0-realtime-slice-harness-rc (version.ts+health+test synced). 81 tests (adds ActiveActionCard 5 + slice-flag 14 + version 22); typecheck 0; production build green."
  ],
  "activeStage": "S5c deploy Preview and falsify the §18 journey by clicking ?voice=realtime2 in the deployed browser (BROWSER/PREVIEW class): greeting-once, WhatsApp card, atomic REPLACE to Call, complaint-no-mutation, fallback preserves the Call action, fabricated-completion blocked. Then update the stable RC alias and run the independent unknown-failure campaign.",
  "nextExactExecutableAction": "Deploy a Vercel Preview of this commit; open the deployed URL /?voice=realtime2, enter AbuAI, and click through the harness (greet -> WhatsApp -> 'לא, תתקשרי אליו' -> complaint -> fallback), recording the state readout (rev/kind/status/supersedes/active/transport) as PREVIEW evidence; then point the stable RC alias at it.",
  "terminalConditions": ["COMPLETION (wired+deployed+falsified)", "EXTERNAL BLOCKER", "ADR CONTRADICTION"],
  "blockers": [
    "The live-audio conversational journey is physical-only (mic) per ADR §20; the AUTOMATABLE deployed proof requires a simulated-realtime test seam on the 2000-line RealtimeVoiceSession that is a multi-file build.",
    "Wiring must be strictly flag-gated (off by default) so the certified default path is never endangered; deploying unproven wiring onto the live default path is forbidden."
  ],
  "rollbackPoints": ["b1e88fc (control plane only)", "this commit (control plane + tools + truth, all additive/unwired)", "de00f27 (product 0.164.0)"],
  "verdictSoFar": "NOT READY — deterministic authority stack (STATE+tools+TRUTH) implemented and proven (31 tests); live-session wiring + deployed falsification remain."
}
