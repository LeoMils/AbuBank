{
  "build": "0.169.0 (slice modules additive, unwired)",
  "claims": [
    { "claim": "Control plane is the single STATE authority and enforces the 15 lifecycle/ordering laws incl. one-greeting, latest-intent-wins, atomic Call<->WhatsApp replace, stale/cross-generation rejection, general-doesn't-mutate, complaint-not-clarification, no-completion status, privacy-by-construction.", "test": "src/screens/AbuAI/realtime/controlPlane.test.ts", "mode": "node deterministic + mutation demos", "result": "PASS 16/16", "limitation": "not yet wired to the live session" },
    { "claim": "Tool dispatch DELEGATES to the kernel (never decides), maps HANDOFF_AVAILABLE->READY_FOR_HANDOFF, exposes no completion status, refuses a phone number in args, scrubs a number label to null, and is idempotent (no duplicate handoff on retry).", "test": "src/screens/AbuAI/realtime/realtimeTools.test.ts", "mode": "node deterministic (injected kernel)", "result": "PASS 9/9", "limitation": "production adapter over buildCommunicationAction not yet wired" },
    { "claim": "Streaming-truth monitor detects fabricated completion ('שלחתי'/'התקשרתי'/'נשלח') always, and unsupported capability denial when the receipt is READY; passes truthful preparation wording (adversarial good).", "test": "src/screens/AbuAI/realtime/truthMonitor.test.ts", "mode": "node deterministic", "result": "PASS 6/6", "limitation": "bounded (Hebrew completion/denial classes); structural avoidance is the primary guarantee; not yet run on live model transcript" },
    { "claim": "Realtime credential + function-tool API verified.", "test": "POST /api/realtime-token (redacted) + OpenAI docs", "mode": "deployed probe + docs", "result": "GREEN (ok:true, gpt-realtime, no quota block; Realtime supports session/response function tools)", "limitation": "secret never printed/committed" },
    { "claim": "S5b: the canonical ActiveActionCard (§13) is a faithful, safe projection of one committed ActiveActionViewModel (message + call revisions; never a completion word; NOT_CONFIGURED shows no handoff button; invisible/no-card renders nothing). The realtime2 slice flag is independent of the realtime beta (proven both directions) and OFF by default. A self-contained RealtimeSliceHarness mounts behind isRealtimeSliceEnabled() (two gated lines in AbuAI/index.tsx) and drives the §18 journey with no mic. Version bumped to 0.170.0-realtime-slice-harness-rc (version.ts+health+test synced).", "test": "src/components/ActiveActionCard.test.tsx + src/services/voiceModePreference.test.ts + src/version.test.ts", "mode": "node deterministic + react-dom/server render", "result": "PASS (81 slice tests; FULL SUITE 11906 passed / 2 todo / 0 failed across 419 files; typecheck 0; production build green)", "limitation": "CODE/BUILD class. The deployed browser click-through of the harness (BROWSER/PREVIEW) is S5c; DEVICE mic naturalness/audibility stays physical-only (§20) and is not claimed." },
    { "claim": "Session orchestrator composes the three authorities (STATE control plane + TRUTH kernel/tools + SPEECH-GUARD monitor) into the ADR §18 journey driven by INJECTED events (no mic/WebRTC): greeting-once, WhatsApp->atomic REPLACE to Call (kind+revision change, supersedes, one card), stale-revision + pre-fallback-generation results rejected, complaint doesn't mutate, fabricated completion blocked before voicing, fallback preserves the Call action, unresolved recipient -> clarification not a guess. Projects ONE ActiveActionViewModel the card + speech both read (law 9). Production kernel adapter binds the dispatcher to the single buildCommunicationAction authority (no fork).", "test": "src/screens/AbuAI/realtime/sessionOrchestrator.test.ts", "mode": "node deterministic (injected kernel)", "result": "PASS 9/9 (40 realtime tests total; typecheck 0 errors)", "limitation": "the simulated-realtime SEAM is proven; the live RealtimeVoiceSession wiring + React card + deployed browser falsification remain (S5b). CODE/MOCK class — not yet BROWSER/PREVIEW/DEVICE." },
    { "claim": "S5c DEPLOYED FALSIFICATION: the §18 journey is proven by clicking in the live Vercel Preview (no mic) — greeting-once, WhatsApp card (rev1/message), 'לא, תתקשרי אליו' atomic REPLACE to Call (rev2/kind flip/supersedes/active=1), complaint no-mutation, speech guard BLOCKS a fabricated completion, card never renders a completion. Deployed buildVersion == tested == pushed (0.170.0). Stable RC alias abu-ela-rc.vercel.app updated to serve it. Independent unknown-failure campaign (adversarial orderings incl. replace-before-start, triple flip, cancel+late-result, out-of-order, fallback/reconnect churn, phone-number-scrub, throwing kernel) found NO unknown failure.", "test": "e2e/realtime-slice-journey.spec.ts (mobile-chrome, PREVIEW_URL) + src/screens/AbuAI/realtime/sessionOrchestratorAdversarial.test.ts", "mode": "deployed browser (Playwright) + node adversarial", "result": "PASS — deployed e2e 1/1 (9.2s) on abu-bank-6hhfwyqb9…vercel.app; adversarial 9/9; /api/health 0.170.0 on both the deployment and abu-ela-rc.vercel.app", "limitation": "BROWSER/PREVIEW class. The live mic->function_call WebRTC path is NOT yet wired (session create_response:false; brain voices answers) — proven via the injectable seam + deployed harness, not a real mic turn. DEVICE mic naturalness/audibility remains physical-only (§20)." }
  ],
  "physicalOnly": ["real Hebrew mic capture", "full-duplex barge-in feel", "perceived naturalness/intonation", "actual WhatsApp launch", "actual dialer launch", "subjective usability", "live mic->function_call WebRTC round-trip (follow-on §17 stage 5; not this slice)"]
}
